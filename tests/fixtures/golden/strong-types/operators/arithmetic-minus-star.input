# Test: Minus with both binary and unary forms
# Feature: -* generates BOTH binary operator- AND unary operator-
# Expected: Binary and unary operator- in generated code

[type]
kind=struct
namespace=test
name=MinusStarType
description=strong int; -*
